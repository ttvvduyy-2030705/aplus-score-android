export type Screens = {
  home: string;
  gameSettings: string;
  gamePlay: string;
  history: string;
  playback: string;
  configs: string;
  overlay: string;
};

export type Scenes = {
  [key: string]: any;
};
